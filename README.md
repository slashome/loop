# Mobile (Flutter)

Starter folder structure for the Flutter app.

- `lib/` application code
- `test/` unit and widget tests
- `assets/` images and fonts

Scaffolding (pubspec.yaml, android/ios project files, etc.) can be added next.

